#include<stdio.h>
#include<string.h>
main()
{
    char s[1000];
    while(1)
    {
        gets(s);
        if(strcmp(s,"DONE")==0)
        return 0;
        for(int i=0;i<strlen(s);i++)
        {
            if(s)
        }
    }
    return 0;
}
